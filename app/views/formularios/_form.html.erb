<%= form_for(@formulario) do |f| %>
  <% if @formulario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@formulario.errors.count, "error") %> prohibited this formulario from being saved:</h2>

      <ul>
      <% @formulario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome  %><br>
    <%= f.text_field :nome, class: "form-control" %>
  </div>

    <hr>
    <div id="add"></div>
    <br>

    <% if txt == "Criar Formulario" %>

        <a id="repeat" class="btn btn-primary">Adicionar mais campos</a>
    <% end %>

    <br><br><hr>
  <div class="actions">
    <%= f.submit txt , class: "btn btn-success" %>
  </div>

<% end %>

